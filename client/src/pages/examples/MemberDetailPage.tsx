import MemberDetailPage from "../MemberDetailPage";
import { ThemeProvider } from "@/components/ThemeProvider";

export default function MemberDetailPageExample() {
  return (
    <ThemeProvider>
      <MemberDetailPage />
    </ThemeProvider>
  );
}
